# Build progress

## M0 — Repo, tooling, CI

- Built: adopted Next.js 16.3.4/App Router, strict TypeScript, Tailwind and existing Supabase helpers; npm lockfile; public configuration validation; Vitest and Playwright tooling; push/PR GitHub quality workflow; complete tracked environment template; five-line README guide.
- Verified: `npm run check` (`npm run lint && npm run typecheck && npm test`) passed, including 13 configuration tests; `npm run build` passed; `npx playwright install chromium` and `npm run e2e` passed (one real Chromium homepage smoke test).
- Installed: `npx --yes skills add supabase/agent-skills --agent codex --yes` completed, installing Supabase and Postgres best-practice skills.
- Cut: none. Hosted GitHub Actions execution cannot be observed until a Git remote is supplied and the repository is pushed; workflow triggers and local commands are in place. This is tracked as OQ002.
- Remaining: M1–M8 and their full verification. No feature milestone is claimed complete by these bootstrap checks.

## M1 — Schema, auth, RLS, audit

- Built: all 18 planned tables plus immutable-summary storage support; Drizzle migrations; separate profile/account IDs; database-enforced adult signup; guardian-owned dependents and consent provenance; 19 RLS-enabled tables; explicit grants and audited scoped read functions; sign-in, confirmation/callback, account, sign-out, and dependent-creation routes.
- Verified: `npm run check` (23 unit tests); `npm run db:verify` (14 real Postgres migration/RLS tests); `npm run db:migrate` applied the migrations; `npm run db:advisors` reported no issues; `npm run build`; `npm run e2e` (3 Chromium tests); `npm run auth:verify` (real Supabase one-time-link session, dependent consent, and sign-out). Synthetic live Auth account and its data were cleaned up.
- Verification scope: migration-from-scratch tests use isolated temporary schemas and roll back all fixtures. Hosted Actions and inbox delivery remain unobserved, tracked in OQ002–OQ003. Guardian conversion UI and doctor-facing PDFs remain assigned to M6.
- Cut: none. M2 follows below.

## M2 — Ingestion

- Built: deterministic simulator, three-persona CLI seed, bounded CSV and streaming Apple ZIP browser-worker adapters, import/cancel/progress UI, CSV template, aggregator interface/stub, stable source/device deduplication, consent/ownership-checked ingestion and durable affected-day summary jobs.
- Verified: `npm run check` (44 unit tests); `npm run db:verify` (25 real Postgres tests, repeated); `npm run db:migrate`; `npm run seed` twice (22,700 total sample readings, zero new readings on the second pass); `npm run build`; `npm run db:advisors` (no issues); `npm audit --omit=dev` (zero vulnerabilities); `HMS_TEST_PORT=3101 npm run e2e` (5 tests); `HMS_TEST_PORT=3101 npm run auth:verify` (1 live Auth test); `npm run ingestion:verify` (real CSV round-trip and two full browser-worker ZIP imports).
- Large-file evidence: 220,201,254 uncompressed XML bytes; 620,285 records per pass; 6,000 distinct readings added on the first pass and none on the second; 1,243 total batches including CSV; maximum payload 260,111 bytes / 1,000 records; peak renderer RSS 256,753,664 bytes (~245 MiB), sampled 1,562 times. No request, browser or memory-sampling errors in the passing run. The synthetic import actor/data were cleaned up; CLI sample personas remain intentionally. Tracked report: `docs/verification/m2-ingestion.json`.
- Fixed during verification: isolated Playwright output directories to prevent parallel suites deleting the import fixture; reset per-sync counters; bounded request deadlines/backoff and correct retryable service-error status. Early failed runs are not counted as passing evidence.
- Remaining: M3–M8. No M3 implementation started before M2 verification passed.
- Cut: none.

## M3 — Analytics and durable recomputation

- Built: pure baselines, sustained anomalies, local-day summaries, explainable recovery/readiness, anchored cycle estimates and every planned insight rule; persisted score evidence/source provenance; leased, retryable summary processing with seven-day coalescing, historical invalidation and atomic audit/completion; timezone-correct sample generation.
- Verified: `npm run check` (73 tests); `npm run db:verify` (27 real Postgres tests, including historical score changes, stale leases, retries and withdrawn consent); `npm run db:migrate` applied migrations 0004–0005; `npm run seed` twice (zero new rows on both confirmation passes); `npm run summaries:recompute` (270 local-day jobs completed, zero failures); `npm run summaries:verify` (all 270 persisted days, baselines and insights match fresh calculations across 22,700 sample readings); `npm run build`; `HMS_TEST_PORT=3101 npm run e2e` (5); `HMS_TEST_PORT=3101 npm run auth:verify` (1); `npm run db:advisors` (no issues).
- Sample results: 90 days and 13 populated baselines each; persona (b) has the cycle insight, persona (c) has strain/temperature insights; latest readiness scores are 100/100/55. No pending sample summary jobs remain.
- Fixed during verification: qualified an ambiguous SQL sort; reduced per-day database round trips through bounded coalescing; regenerated only the versioned synthetic fixtures after detecting UTC samples spilling into a 91st local day. Their data are reproducible via the seed; uploaded data were not removed.
- Cut: none. Every-minute dispatch remains M4; M3 verifies the durable processor through its CLI. M4 has not started before these gates passed.

## M4 — Alerts and minute dispatch

- Built: all eight default rules and owner overrides; exact alert copy and source/rest/measurement evidence; persisted 15-minute deadlines; consent-checked, leased notification outbox with idempotent retries; authenticated bounded minute dispatcher; Supabase Cron/Vault setup and same-cadence Vercel fallback template; acknowledgement/contact settings; Chrome worker registration and local notification test.
- Verified: `npm run check` (86 unit tests); `npm run db:verify` (40 real Postgres tests, including fake-clock deadlines, acknowledgement/consent revocation, overlapping dispatches, retry fencing and caregiver scope); `npm run db:migrate` applied 0006–0008; `npm run summaries:recompute` (270 jobs, zero failures); `npm run summaries:verify` (all 270 sample days still match); `npm run build`; `HMS_TEST_PORT=3102 npm run alerts:verify` (one live 390-px Chromium flow, actual local notification, two actual console-stub deliveries and persisted acknowledgement); `HMS_TEST_PORT=3101 npm run e2e` (5); `HMS_TEST_PORT=3101 npm run auth:verify` (1); `npm run db:advisors` (no issues); `npm audit --omit=dev` (zero vulnerabilities).
- Fixed during verification: optimistic consent controls with rollback; use full Chromium for its actual notification permission support; reject future/unfinished imported measurements; preserve profile-first lock order during delivery completion. Early failed browser runs are not passing evidence. Synthetic live-test accounts/data were cleaned up.
- Deployment dependencies: scheduler setup is implemented but not activated against localhost. A deployed URL and cron secret are required; minute-route invocation is verified locally. Resend is currently the permitted console stub. Server Web Push, SMS and WhatsApp remain explicitly labelled stubs; the Chrome test verifies a local notification, not server delivery. See OQ004.
- Cut: no additional cuts. The founder approved milestone-aligned golden-path gates in D009; M5 starts only after these M4 checks and commit.

## M5 — Patient UI and installability

- Built: four-step onboarding; three-tab mobile shell; priority Today card and bounded insights; gap-aware History with cycle opt-in/shading, alert acknowledgement markers, source inspection and paged full history; More, raw records, baselines, cycle settings, imports and processing-consent withdrawal; bounded owner-only interactive summary refresh; manifest, generated PNG icons and non-caching service worker registration.
- Verified: `npm run check` (90 unit tests); `npm run db:verify` (47 real Postgres tests, including scoped/audited views, 401-day pagination and owner-only job claims); `npm run db:migrate` applied 0009–0011; `npm run build`; `HMS_TEST_PORT=3103 npm run golden:verify` (paths 1–2, real Supabase, 390-px viewport, no horizontal overflow, cycle/source inspection, acknowledgement and consent withdrawal); `HMS_TEST_PORT=3101 npm run e2e` (6); `HMS_TEST_PORT=3101 npm run auth:verify` (1); `HMS_TEST_PORT=3102 npm run alerts:verify` (1); `npm run db:advisors` (no issues).
- PWA evidence: Chrome 153.0.8010.12, isolated disposable non-incognito profile; manifest errors and installability errors both empty; actual 192/512-px PNG dimensions verified; active service worker and zero cache entries. Mobile screenshots were visually inspected. This is a current Chrome installability check, not a claim that the deprecated Lighthouse PWA audit or physical-device tests ran. Tracked summary: `docs/verification/m5-patient-ui.json`.
- Fixed during verification: React SVG-title hydration, summary refresh under Strict Mode, exact acknowledgement payload, same-source day selection and request-time local dates. Browser fixtures are synthetic and removed after each test. Failed exploratory runs are not passing evidence.
- Cut: no additional cuts. Post-import timezone changes remain explicitly unavailable until a tested re-bucketing workflow (OQ005). Native controls/shared CSS and Chrome's replacement installability check are recorded in D010. Doctor workflows and their golden paths remain M6; export/deletion remain M7.

## M6 — Doctor, caregiver and guardian workflows

- Built: pending/verified doctor registration with admin-only verification; fictional verified/pending doctor seeds; consented summary/full-history/alerts sharing; scoped doctor portal and consult queue; immutable 30/90-day clinical summaries/PDFs; reported medication lists; accept/schedule/message/close consult flow and completed notes in History; account-code caregiver invitations, acceptance and revocation; dependent management and explicit adult-account ownership transfer.
- Verified: `npm run check` (93 unit tests); `npm run db:verify` (78 real Postgres tests, all 22 tables migrated from scratch with RLS); `npm run db:migrate` applied 0012–0018; `npm run db:advisors` (no issues); `npm run seed` (zero new sample readings; verified/pending doctor fixtures retained); `npm run summaries:verify` (all 270 sample days and baselines/insights match); `npm run build` (39 routes); `HMS_PDFTOTEXT=<Poppler pdftotext> HMS_TEST_PORT=3103 npm run golden:verify` (all six enabled paths 1–4, 6–7 pass together); `HMS_TEST_PORT=3101 npm run e2e` (6), `HMS_TEST_PORT=3101 npm run auth:verify` (1), `HMS_TEST_PORT=3102 npm run alerts:verify` (1); `npm audit --omit=dev` (zero vulnerabilities).
- PDF verification: `HMS_PDFTOTEXT=<Poppler pdftotext> npm run pdf:verify`, with Poppler's `pdfinfo`/`pdftoppm` on PATH. Three personas plus guardian, hostile-width and Latin/Devanagari fixtures each produced one page. Medication text retention and unsupported-character refusal are asserted. All six rendered PNGs, live guardian PDF and mobile clinical/consult/family screens were visually inspected. Both local font files appear in Next's production route dependency trace. Tracked evidence: `docs/verification/m6-care-team.json`.
- Golden evidence: actual patient/doctor private PDF downloads; summary-only raw-History denial; both consult participants exchange messages and History shows the closing note; caregiver email forwarding uses the actual privacy-preserving stub and persisted outbox status; three explicit caregiver reads produce three audit rows; revoked access fails within one page load. A minor cannot sign up, a dependent has no Auth identity, and guardian data remains separate. The controlled-clock conversion keeps history IDs and consent provenance; an additional synthetic adult fixture exercises the real offer/accept UI and production route, including all four confirmations. Established recipient profiles are refused without replacement.
- Fixed during verification: ESM/CommonJS CLI interop; explicit accessible scope-selector name; long/unbroken PDF text overflow, embedded Unicode fonts and unsupported-glyph refusal. Failed exploratory runs are not counted as passing evidence. Live fixture accounts/data were removed; reserved CLI sample personas/doctors remain deliberately.
- Limits/cuts: chat uses explicit refresh, not realtime subscriptions. Video is a doctor-supplied Meet/Zoom link, not a provisioned service. Invitations use account codes and no email claim. PDF lists six complete medication entries/six recent document titles with explicit remaining counts; full originals remain in the app. Additional PDF scripts require verified fonts (OQ006). Parental/legal verification, real notification credentials and deployment configuration remain OQ001–OQ004. Export/deletion and the seventh golden path remain M7.

## M7 — Verified: data rights, documents, legal, catalogue and deployment fallback

- Built in `/Users/gari/Documents/ChatGPT/HMS-m7` on `codex/m7-data-rights-catalog`. The shared original checkout was switched externally to M4; it remains untouched. Advance `main` from verified M6 (`a3abd1c`) only after the M7 commit.
- Built: streamed per-metric ZIP/JSONL/original-document export for owned profiles only; checked/audited private document upload/download; durable retryable account-deletion fence, Storage purge, health purge/audit anonymisation and Auth deletion; protection of other patients' consultation history; real privacy/terms and pharmacy link-only stubs; 20 dated manufacturer-sourced catalogue rows with currency/budget, must-have features, battery and screen filtering.
- Verified: `npm run check` (107 unit tests), `npm run db:verify` (87 real database tests, all migrations from scratch and all 23 tables protected by RLS), `npm run db:migrate` (through 0022), `npm run storage:setup` (private 3 MiB PDF/JPEG/PNG bucket), `npm run db:advisors` (no issues), `npm run build` (47 routes), `npm run seed:devices` repeatedly (20 rows round-trip as anonymous, no duplicates), `HMS_TEST_PORT=3108 npm run e2e` (8, also pass with CI's placeholder public configuration), `npm audit --omit=dev` (0 vulnerabilities), `npm run pdf:verify` with documented Poppler overrides (six one-page fixtures). Document History, catalogue, privacy, terms and pharmacy screenshots reviewed at 390 px without horizontal overflow. Synthetic accounts/files cleaned up.
- Final production smoke: `HMS_TEST_PORT=3107 HMS_PDFTOTEXT=/Users/gari/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/poppler/poppler/bin/pdftotext FONTCONFIG_FILE=/Users/gari/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/poppler/poppler/etc/fonts/fonts.conf npm run golden:verify` — all 9 tests pass (all seven paths, interrupted-deletion retry and live catalogue). Includes cross-user/direct Storage denial, ZIP originals, actual Auth/Storage/health deletion, stale-token rejection, audited doctor/caregiver access, guardian conversion, patient charts and PWA installability.
- Additional regression: `HMS_TEST_PORT=3109 npm run auth:verify` (1) and `HMS_TEST_PORT=3109 npm run alerts:verify` (1) pass. `npm run seed && npm run summaries:recompute && npm run summaries:verify` inserts no duplicate sample readings: 22,700 duplicates skipped, 90 summary days/13 baselines per persona, readiness 100/100/55 and no pending jobs. CI's plain-Postgres bootstrap now includes a minimal provider-owned Storage table so the restrictive policy can replay; live Storage APIs are verified separately, not simulated.
- Regression corrections: the first complete run lacked Poppler on PATH; use `HMS_PDFTOTEXT` for the guardian PDF. The consult-message assertion could match composer text before persistence; it now waits for a saved message bubble. Catalogue's ambiguous nested screen label was corrected and the currency assertion matches browser formatting. No tests skipped or golden paths removed.
- Cuts/limits: Vercel CLI 59.11.7 reports logged out, so PLAN's production-build/local-smoke fallback is used (D017); no public deployment or minute scheduler is claimed. Legal launch dependencies remain OQ001/OQ007, real notifications remain OQ004 and Apollo requires manual search-term entry (D015). Unknown catalogue prices/features stay explicitly unverified. M8 review and two complete verification passes remain before the overall goal is complete.

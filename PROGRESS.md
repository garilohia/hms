# Build progress

## M0 — Repo, tooling, CI

- Built: adopted Next.js 16.3.4/App Router, strict TypeScript, Tailwind and existing Supabase helpers; npm lockfile; public configuration validation; Vitest and Playwright tooling; push/PR GitHub quality workflow; complete tracked environment template; five-line README guide.
- Verified: `npm run check` (`npm run lint && npm run typecheck && npm test`) passed, including 13 configuration tests; `npm run build` passed; `npx playwright install chromium` and `npm run e2e` passed (one real Chromium homepage smoke test).
- Installed: `npx --yes skills add supabase/agent-skills --agent codex --yes` completed, installing Supabase and Postgres best-practice skills.
- Cut: none. Hosted GitHub Actions execution cannot be observed until a Git remote is supplied and the repository is pushed; workflow triggers and local commands are in place. This is tracked as OQ002.
- Remaining: M1–M8 and their full verification. No feature milestone is claimed complete by these bootstrap checks.

## M1 — Schema, auth, RLS, audit

- Built: all 18 planned tables plus immutable-summary storage support; Drizzle migrations; separate profile/account IDs; database-enforced adult signup; guardian-owned dependents and consent provenance; 19 RLS-enabled tables; explicit grants and audited scoped read functions; sign-in, confirmation/callback, account, sign-out, and dependent-creation routes.
- Verified: `npm run check` (23 unit tests); `npm run db:verify` (14 real Postgres migration/RLS tests); `npm run db:migrate` applied the migrations; `npm run db:advisors` reported no issues; `npm run build`; `npm run e2e` (3 Chromium tests); `npm run auth:verify` (real Supabase one-time-link session, dependent consent, and sign-out). Synthetic live Auth account and its data were cleaned up.
- Verification scope: migration-from-scratch tests use isolated temporary schemas and roll back all fixtures. Hosted Actions and inbox delivery remain unobserved, tracked in OQ002–OQ003. Guardian conversion UI and doctor-facing PDFs remain assigned to M6.
- Cut: none. M2 follows below.

## M2 — Ingestion

- Built: deterministic simulator, three-persona CLI seed, bounded CSV and streaming Apple ZIP browser-worker adapters, import/cancel/progress UI, CSV template, aggregator interface/stub, stable source/device deduplication, consent/ownership-checked ingestion and durable affected-day summary jobs.
- Verified: `npm run check` (44 unit tests); `npm run db:verify` (25 real Postgres tests, repeated); `npm run db:migrate`; `npm run seed` twice (22,700 total sample readings, zero new readings on the second pass); `npm run build`; `npm run db:advisors` (no issues); `npm audit --omit=dev` (zero vulnerabilities); `HMS_TEST_PORT=3101 npm run e2e` (5 tests); `HMS_TEST_PORT=3101 npm run auth:verify` (1 live Auth test); `npm run ingestion:verify` (real CSV round-trip and two full browser-worker ZIP imports).
- Large-file evidence: 220,201,254 uncompressed XML bytes; 620,285 records per pass; 6,000 distinct readings added on the first pass and none on the second; 1,243 total batches including CSV; maximum payload 260,111 bytes / 1,000 records; peak renderer RSS 256,753,664 bytes (~245 MiB), sampled 1,562 times. No request, browser or memory-sampling errors in the passing run. The synthetic import actor/data were cleaned up; CLI sample personas remain intentionally. Tracked report: `docs/verification/m2-ingestion.json`.
- Fixed during verification: isolated Playwright output directories to prevent parallel suites deleting the import fixture; reset per-sync counters; bounded request deadlines/backoff and correct retryable service-error status. Early failed runs are not counted as passing evidence.
- Remaining: M3–M8. No M3 implementation started before M2 verification passed.
- Cut: none.

## M3 — Analytics and durable recomputation

- Built: pure baselines, sustained anomalies, local-day summaries, explainable recovery/readiness, anchored cycle estimates and every planned insight rule; persisted score evidence/source provenance; leased, retryable summary processing with seven-day coalescing, historical invalidation and atomic audit/completion; timezone-correct sample generation.
- Verified: `npm run check` (73 tests); `npm run db:verify` (27 real Postgres tests, including historical score changes, stale leases, retries and withdrawn consent); `npm run db:migrate` applied migrations 0004–0005; `npm run seed` twice (zero new rows on both confirmation passes); `npm run summaries:recompute` (270 local-day jobs completed, zero failures); `npm run summaries:verify` (all 270 persisted days, baselines and insights match fresh calculations across 22,700 sample readings); `npm run build`; `HMS_TEST_PORT=3101 npm run e2e` (5); `HMS_TEST_PORT=3101 npm run auth:verify` (1); `npm run db:advisors` (no issues).
- Sample results: 90 days and 13 populated baselines each; persona (b) has the cycle insight, persona (c) has strain/temperature insights; latest readiness scores are 100/100/55. No pending sample summary jobs remain.
- Fixed during verification: qualified an ambiguous SQL sort; reduced per-day database round trips through bounded coalescing; regenerated only the versioned synthetic fixtures after detecting UTC samples spilling into a 91st local day. Their data are reproducible via the seed; uploaded data were not removed.
- Cut: none. Every-minute dispatch remains M4; M3 verifies the durable processor through its CLI. M4 has not started before these gates passed.

## M4 — Alerts and minute dispatch

- Built: all eight default rules and owner overrides; exact alert copy and source/rest/measurement evidence; persisted 15-minute deadlines; consent-checked, leased notification outbox with idempotent retries; authenticated bounded minute dispatcher; Supabase Cron/Vault setup and same-cadence Vercel fallback template; acknowledgement/contact settings; Chrome worker registration and local notification test.
- Verified: `npm run check` (86 unit tests); `npm run db:verify` (40 real Postgres tests, including fake-clock deadlines, acknowledgement/consent revocation, overlapping dispatches, retry fencing and caregiver scope); `npm run db:migrate` applied 0006–0008; `npm run summaries:recompute` (270 jobs, zero failures); `npm run summaries:verify` (all 270 sample days still match); `npm run build`; `HMS_TEST_PORT=3102 npm run alerts:verify` (one live 390-px Chromium flow, actual local notification, two actual console-stub deliveries and persisted acknowledgement); `HMS_TEST_PORT=3101 npm run e2e` (5); `HMS_TEST_PORT=3101 npm run auth:verify` (1); `npm run db:advisors` (no issues); `npm audit --omit=dev` (zero vulnerabilities).
- Fixed during verification: optimistic consent controls with rollback; use full Chromium for its actual notification permission support; reject future/unfinished imported measurements; preserve profile-first lock order during delivery completion. Early failed browser runs are not passing evidence. Synthetic live-test accounts/data were cleaned up.
- Deployment dependencies: scheduler setup is implemented but not activated against localhost. A deployed URL and cron secret are required; minute-route invocation is verified locally. Resend is currently the permitted console stub. Server Web Push, SMS and WhatsApp remain explicitly labelled stubs; the Chrome test verifies a local notification, not server delivery. See OQ004.
- Cut: no additional cuts. The founder approved milestone-aligned golden-path gates in D009; M5 starts only after these M4 checks and commit.

## M5 — Patient UI and installability

- Built: four-step onboarding; three-tab mobile shell; priority Today card and bounded insights; gap-aware History with cycle opt-in/shading, alert acknowledgement markers, source inspection and paged full history; More, raw records, baselines, cycle settings, imports and processing-consent withdrawal; bounded owner-only interactive summary refresh; manifest, generated PNG icons and non-caching service worker registration.
- Verified: `npm run check` (90 unit tests); `npm run db:verify` (47 real Postgres tests, including scoped/audited views, 401-day pagination and owner-only job claims); `npm run db:migrate` applied 0009–0011; `npm run build`; `HMS_TEST_PORT=3103 npm run golden:verify` (paths 1–2, real Supabase, 390-px viewport, no horizontal overflow, cycle/source inspection, acknowledgement and consent withdrawal); `HMS_TEST_PORT=3101 npm run e2e` (6); `HMS_TEST_PORT=3101 npm run auth:verify` (1); `HMS_TEST_PORT=3102 npm run alerts:verify` (1); `npm run db:advisors` (no issues).
- PWA evidence: Chrome 153.0.8010.12, isolated disposable non-incognito profile; manifest errors and installability errors both empty; actual 192/512-px PNG dimensions verified; active service worker and zero cache entries. Mobile screenshots were visually inspected. This is a current Chrome installability check, not a claim that the deprecated Lighthouse PWA audit or physical-device tests ran. Tracked summary: `docs/verification/m5-patient-ui.json`.
- Fixed during verification: React SVG-title hydration, summary refresh under Strict Mode, exact acknowledgement payload, same-source day selection and request-time local dates. Browser fixtures are synthetic and removed after each test. Failed exploratory runs are not passing evidence.
- Cut: no additional cuts. Post-import timezone changes remain explicitly unavailable until a tested re-bucketing workflow (OQ005). Native controls/shared CSS and Chrome's replacement installability check are recorded in D010. Doctor workflows and their golden paths remain M6; export/deletion remain M7.

## M6 — Doctor, caregiver and guardian workflows

- Built: pending/verified doctor registration with admin-only verification; fictional verified/pending doctor seeds; consented summary/full-history/alerts sharing; scoped doctor portal and consult queue; immutable 30/90-day clinical summaries/PDFs; reported medication lists; accept/schedule/message/close consult flow and completed notes in History; account-code caregiver invitations, acceptance and revocation; dependent management and explicit adult-account ownership transfer.
- Verified: `npm run check` (93 unit tests); `npm run db:verify` (78 real Postgres tests, all 22 tables migrated from scratch with RLS); `npm run db:migrate` applied 0012–0018; `npm run db:advisors` (no issues); `npm run seed` (zero new sample readings; verified/pending doctor fixtures retained); `npm run summaries:verify` (all 270 sample days and baselines/insights match); `npm run build` (39 routes); `HMS_PDFTOTEXT=<Poppler pdftotext> HMS_TEST_PORT=3103 npm run golden:verify` (all six enabled paths 1–4, 6–7 pass together); `HMS_TEST_PORT=3101 npm run e2e` (6), `HMS_TEST_PORT=3101 npm run auth:verify` (1), `HMS_TEST_PORT=3102 npm run alerts:verify` (1); `npm audit --omit=dev` (zero vulnerabilities).
- PDF verification: `HMS_PDFTOTEXT=<Poppler pdftotext> npm run pdf:verify`, with Poppler's `pdfinfo`/`pdftoppm` on PATH. Three personas plus guardian, hostile-width and Latin/Devanagari fixtures each produced one page. Medication text retention and unsupported-character refusal are asserted. All six rendered PNGs, live guardian PDF and mobile clinical/consult/family screens were visually inspected. Both local font files appear in Next's production route dependency trace. Tracked evidence: `docs/verification/m6-care-team.json`.
- Golden evidence: actual patient/doctor private PDF downloads; summary-only raw-History denial; both consult participants exchange messages and History shows the closing note; caregiver email forwarding uses the actual privacy-preserving stub and persisted outbox status; three explicit caregiver reads produce three audit rows; revoked access fails within one page load. A minor cannot sign up, a dependent has no Auth identity, and guardian data remains separate. The controlled-clock conversion keeps history IDs and consent provenance; an additional synthetic adult fixture exercises the real offer/accept UI and production route, including all four confirmations. Established recipient profiles are refused without replacement.
- Fixed during verification: ESM/CommonJS CLI interop; explicit accessible scope-selector name; long/unbroken PDF text overflow, embedded Unicode fonts and unsupported-glyph refusal. Failed exploratory runs are not counted as passing evidence. Live fixture accounts/data were removed; reserved CLI sample personas/doctors remain deliberately.
- Limits/cuts: chat uses explicit refresh, not realtime subscriptions. Video is a doctor-supplied Meet/Zoom link, not a provisioned service. Invitations use account codes and no email claim. PDF lists six complete medication entries/six recent document titles with explicit remaining counts; full originals remain in the app. Additional PDF scripts require verified fonts (OQ006). Parental/legal verification, real notification credentials and deployment configuration remain OQ001–OQ004. Export/deletion and the seventh golden path remain M7.

## M7 — Verified: data rights, documents, legal, catalogue and deployment fallback

- Built in `/Users/gari/Documents/ChatGPT/HMS-m7` on `codex/m7-data-rights-catalog`. The shared original checkout was switched externally to M4; it remains untouched. Advance `main` from verified M6 (`a3abd1c`) only after the M7 commit.
- Built: streamed per-metric ZIP/JSONL/original-document export for owned profiles only; checked/audited private document upload/download; durable retryable account-deletion fence, Storage purge, health purge/audit anonymisation and Auth deletion; protection of other patients' consultation history; real privacy/terms and pharmacy link-only stubs; 20 dated manufacturer-sourced catalogue rows with currency/budget, must-have features, battery and screen filtering.
- Verified: `npm run check` (107 unit tests), `npm run db:verify` (87 real database tests, all migrations from scratch and all 23 tables protected by RLS), `npm run db:migrate` (through 0022), `npm run storage:setup` (private 3 MiB PDF/JPEG/PNG bucket), `npm run db:advisors` (no issues), `npm run build` (47 routes), `npm run seed:devices` repeatedly (20 rows round-trip as anonymous, no duplicates), `HMS_TEST_PORT=3108 npm run e2e` (8, also pass with CI's placeholder public configuration), `npm audit --omit=dev` (0 vulnerabilities), `npm run pdf:verify` with documented Poppler overrides (six one-page fixtures). Document History, catalogue, privacy, terms and pharmacy screenshots reviewed at 390 px without horizontal overflow. Synthetic accounts/files cleaned up.
- Final production smoke: `HMS_TEST_PORT=3107 HMS_PDFTOTEXT=/Users/gari/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/poppler/poppler/bin/pdftotext FONTCONFIG_FILE=/Users/gari/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/poppler/poppler/etc/fonts/fonts.conf npm run golden:verify` — all 9 tests pass (all seven paths, interrupted-deletion retry and live catalogue). Includes cross-user/direct Storage denial, ZIP originals, actual Auth/Storage/health deletion, stale-token rejection, audited doctor/caregiver access, guardian conversion, patient charts and PWA installability.
- Additional regression: `HMS_TEST_PORT=3109 npm run auth:verify` (1) and `HMS_TEST_PORT=3109 npm run alerts:verify` (1) pass. `npm run seed && npm run summaries:recompute && npm run summaries:verify` inserts no duplicate sample readings: 22,700 duplicates skipped, 90 summary days/13 baselines per persona, readiness 100/100/55 and no pending jobs. CI's plain-Postgres bootstrap now includes a minimal provider-owned Storage table so the restrictive policy can replay; live Storage APIs are verified separately, not simulated.
- Regression corrections: the first complete run lacked Poppler on PATH; use `HMS_PDFTOTEXT` for the guardian PDF. The consult-message assertion could match composer text before persistence; it now waits for a saved message bubble. Catalogue's ambiguous nested screen label was corrected and the currency assertion matches browser formatting. No tests skipped or golden paths removed.
- Cuts/limits: Vercel CLI 59.11.7 reports logged out, so PLAN's production-build/local-smoke fallback is used (D017); no public deployment or minute scheduler is claimed. Legal launch dependencies remain OQ001/OQ007, real notifications remain OQ004 and Apollo requires manual search-term entry (D015). Unknown catalogue prices/features stay explicitly unverified.

## M8 — Complete: review, hardening and repeat verification

- M7 committed as `76472ea`; `main` advanced to that verified commit. M8 continued in the same isolated worktree on `codex/m8-hardening`; the original M4 checkout remained untouched.
- Entry plan: whole-repository review, fixes for confirmed bugs, then two complete verification runs including the large streaming import and all seven golden paths. Write FINAL_REPORT.md only after recording the actual outcomes and launch limitations.
- Hardening corrections: lost-response fault injection reproduced duplicate consults (two instead of one). Component-lifetime idempotency IDs, confirmed-message draft clearing and strict pagination cursor validation now pass the focused doctor flows; five new unit cases bring the total to 112. Whole-repository `codex review` reported no findings; independent corrections are recorded in `docs/verification/m8-review.md` and D018.
- A first full-run attempt passed build/unit/database/advisor/storage/seed checks, anonymous/Auth/alerts and seven of nine golden tests, but later Auth calls and import requests failed during the concurrent live runs. Both suites failed and are not counted as clean passes. Subsequent direct Auth/REST reads returned 200 with no blocked database sessions; diagnostics do not establish the original service failure. The two exact synthetic account/profile/metric fixtures left by failed cleanup were removed and their absence verified. Repeat the live suites sequentially; never relax assertions or count the interrupted import as passing memory/deduplication evidence.
- The sequential database rerun hit a 30-second alert-race test timeout and a subsequent aborted transaction (85/87 passed). Both affected tests then passed in a focused diagnostic run under the unchanged timeout, about 10 seconds each. Direct database round trips measured 45–85 ms after connection setup. No delivery-code change or timeout increase was made; the filtered diagnostic run does not count as full verification. Continue full, unfiltered suites with per-test database reporting.
- The next unfiltered database run passed all 87 tests (206.75 s), with no advisor issues. Storage, seed, summaries, audit, anonymous/Auth/alerts, all nine golden tests (6.5 min) and all six one-page PDFs also passed. The large import passed its first complete import but hit the test-only 400-second deadline at 570,000 duplicates in the second pass, with no request/browser/memory-sampling errors and 235,601,920-byte peak RSS. That incomplete benchmark is not counted. D019 replaces the undocumented throughput deadline with a bounded, progress-aware allowance; all substantive M2 assertions remain unchanged. Guardian and multilingual PDF PNGs were visually rechecked.
- The next benchmark was interrupted by confirmed host sleep: macOS recorded Clamshell Sleep at 10:49:30 IST, and the progress log jumped from 62 to 1,052 elapsed seconds. It failed rather than ignoring the pause; its exact leftover synthetic Auth/profile/metric fixture was removed and absence verified. Run remaining verification under a process-scoped `caffeinate -i` assertion on this Mac. It prevents idle sleep only, does not change persistent power settings or display locking, and does not override lid-closed sleep. This host interruption is not attributed to application logic or counted as a passing benchmark.
- A subsequent `caffeinate -i` benchmark completed its first import (6,000 inserted / 614,285 skipped, 422,461 ms), then was interrupted again. Power logs confirm Clamshell Sleep at 12:48:39 IST; Chromium recorded `ERR_NETWORK_IO_SUSPENDED`, and the 25-minute test envelope expired. No complete M8 benchmark pass was claimed at that point. The exact leftover synthetic fixture was removed and its Auth/profile/metrics absence verified. Local `npm run check` (112 tests), `npm run build` (47 routes) and `git diff --check` passed; the temporary host-window issue was recorded as OQ008 while M8 remained uncommitted.
- Further shorter rechecks: `HMS_TEST_PORT=3107 npm run e2e` (8), `npm run auth:verify` (1) and `npm run alerts:verify` (1) passed again under a process-scoped idle-sleep assertion. The golden rerun passed eight tests before the last patient path was interrupted: Clamshell Sleep at 13:18:57 IST matches `/api/patient/refresh: net::ERR_NETWORK_IO_SUSPENDED`. The suite failed; the subsequent PDF command did not run. Its exact remaining synthetic account/profile/metric fixture was removed and checked absent. The earlier complete nine-test golden and six-fixture PDF passes remained valid individual results, but the two full-suite passes had not yet been achieved.
- On resumption, an uninterrupted benchmark completed its first import in 544,639 ms (6,000 inserted / 614,285 skipped). Its re-import was still progressing at 511,000/620,285 records when the test-only ten-minute allowance expired at 600,216 ms. That run failed and is not evidence of deduplication completion. The immediately started repeat was manually interrupted to avoid another known timeout; its exact remaining synthetic fixture was removed. D019 now uses a fifteen-minute per-import ceiling and 35-minute test envelope, retaining the two-minute no-progress watchdog and every substantive assertion.
- The fifteen-minute repeat attempt was also invalidated by host sleep. It progressed normally for six minutes to 291,000 records, then the next report jumped to 1,396 elapsed seconds. macOS confirms Clamshell Sleep at 14:06:01 IST followed by repeated maintenance sleeps; `caffeinate -i` cannot override lid closure. The first execution failed during post-sleep cleanup and the second could not create its Auth fixture while the network remained suspended. Both failed; neither counts. The second run was stopped, its sole exact leftover synthetic fixture was removed and its Auth/profile/metrics absence verified. No further long-run retry is useful until the lid can remain open and the Mac online for the whole 40–50-minute window.
- With the lid open and network available, `HMS_TEST_PORT=3107 /usr/bin/caffeinate -i npm run ingestion:verify -- --repeat-each=2` passed both complete tests in 36.7 minutes. Each test parsed a 220,201,254-byte XML fixture with 620,285 records, inserted exactly 6,000 distinct readings on its first import and zero on re-import, used 1,243 bounded batches and three workers, and stayed below the 512 MiB renderer ceiling. Peak renderer RSS was 194,609,152 and 236,044,288 bytes. Evidence is preserved in `docs/verification/m8-ingestion.json`; no synthetic fixture remains.
- The entire shorter verification matrix then passed twice, sequentially and unchanged: `npm run check` (112 tests), production build (47 routes), all 87 database/RLS checks, database advisors, private Storage setup, idempotent seed, summaries, production dependency audit, eight anonymous browser tests, Auth, alerts, all nine golden tests and all six PDF fixtures, followed by `git diff --check`. The golden passes took 7.3 and 7.0 minutes. No test was skipped. OQ008 was resolved and removed; the earlier interrupted attempts remain here as failure history rather than passing evidence.
- `FINAL_REPORT.md` records the completed scope, evidence, honest cuts and founder launch steps. M8 is committed only after the final repository audit, and `main` advances only to that verified commit; the separate original M4 checkout remains untouched.

## Post-M8 (1) — Google Fit/health CSV folder import — 9–10 September 2026

- Added recursive folder selection alongside the existing single Apple Health ZIP/CSV picker. Up to 20,000 CSV files are sorted and streamed sequentially through the existing Web Worker; source files remain on-device and API batch/security boundaries are unchanged.
- The CSV adapter auto-detects the HMS template, legacy Google Fit Daily activity metrics, and Google Health/Fitbit Takeout canonical physical-activity files. The Takeout mapping covers heart rate, resting heart rate, steps, active/total calories, RMSSD HRV, SpO₂, respiratory rate, skin temperature and weight with exact unit conversion. Date-only legacy rows use the profile timezone; explicit offsets are preserved. Consolidated legacy metrics take precedence over date-named siblings. Google Health account/settings files, empty datasets and duplicate summary formats remain skipped with bounded diagnostics; the UI distinguishes recognised health files from skipped files.
- Added a wearable connection hub with OAuth-ready Google Health (Fitbit Air, Fitbit and Pixel Watch) and WHOOP links, honest Apple Health export support, and partner-gated Garmin/Ultrahuman states. Provider tokens are encrypted server-side in a forced-RLS private table and every connection is bound to current ownership and processing consent. OAuth buttons remain disabled until founder-supplied provider credentials and the token key are configured.
- Verification after the Google Health extension: all 446 headers in the supplied Takeout folder were checked locally without printing health values; 35 canonical health CSVs across all ten supported metric families were recognised. `npm run check` passed with 125 unit tests and `npm run build` with 47 routes at that commit; both counts have since grown, and the current figures are in the Post-M8 (2) section below. The focused live browser test imports legacy Google Fit plus Google Health heart-rate/weight files, verifies persistence, then re-imports with zero inserts and ten duplicates. Its synthetic account was removed.
- A real 296,716-reading Google Health folder exposed round-trip latency in the original strictly sequential uploader. Persistence now uses a three-request bounded pipeline while retaining 1,000-reading/1 MiB API limits, idempotency, cancellation and browser-memory backpressure.
- Added a development-only test-login button backed by a real Supabase magic-link session. It reuses a reserved synthetic profile, sends no email, remains subject to RLS, and is absent/404 in production.

## Post-M8 (2) — Native readiness, measured latency and safety semantics — 10 September 2026

- Added a versioned bearer-authenticated mobile bootstrap, native source registration and immutable batch API for future HealthKit and Health Connect clients. Native batches reuse consent, ownership, canonical-unit, future-time, deduplication, summary and alert boundaries.
- Added private batch receipts with exact-retry results and changed-content refusal, plus an audited 30-day latency report and `/more/advanced/latency` UI for source freshness, per-metric source delay, alert creation and completed delivery timing.
- Documented encrypted offline queue, token refresh, background scheduling, health-value-free push and physical-device acceptance requirements for iOS and Android. No native binary is claimed because this host lacks full Xcode and Android toolchains.
- Recorded clinical review, supervised pilot and Vercel Preview environment parity as external launch gates. Missing data is explicitly treated as a feed state, never a medical inference.
- Added physical-device native acceptance, supervised-pilot, clinical/legal sign-off and provider-connector definition-of-done checklists. External approvals and signed mobile binaries remain pending rather than being marked complete in code.
- Verified: `npm run check` (lint, strict typecheck, 132 unit tests) and `npm run build` (54 routes) pass; re-run and confirmed on 11 September 2026. The database, live browser, golden, PDF and large-import evidence for this pass is recorded in FINAL_REPORT.md under “Post-M8 native/freshness verification” and was not re-executed on 11 September 2026.
- Cut: no signed native binary, vendor approval, supervised pilot or clinical/legal approval is claimed.

## Design system pass — 11 September 2026

- Built: DESIGN.md is the locked visual system. Tokens for both modes with a gradient ground, three surfaces and separate `--accent`/`--data` roles are wired into the Tailwind v4 theme and shadcn variable names; `scripts/check-contrast.ts` runs inside `npm run check` and recomputes every §4.5 pair (all match the documented figures). Primitives: lit-edge cards, sage buttons, icon tab bar, chips, inputs and the two alert variants (top rule and label, no wash or full border). A shared `DataChart` implements §5: band-only fill, boundary-split line, serif end label, dashed building state with the day count, square bars, `--urgent` axis notches, aria-label sentence. Today follows the reference screen: resting-heart-rate card with sparkline, Sleep card with bars, alert card, insights, button, plus the date and freshness line. Every route uses the primitives; IBM Plex Sans and Source Serif 4 are self-hosted with `font-display: swap`, system-ui in the fallback stack and tabular numerals on the root. No hex value exists outside `src/lib/design/tokens.ts` and `app/globals.css`.
- Verified: `npm run check` (lint, strict typecheck, contrast check, 135 unit tests) and `npm run build` (54 routes) after each step; `HMS_PDFTOTEXT=<Poppler pdftotext> FONTCONFIG_FILE=<Poppler fonts.conf> HMS_TEST_PORT=3102 npm run golden:verify` (all 10 tests, 4.3 min) on the final build; `HMS_TEST_PORT=3101 npm run design:audit` (66 screenshots at 390px in light and dark, no horizontal scroll, no page errors, `design-audit/manifest.json`). The §13 walk is in `DESIGN_AUDIT.md`. The first full golden run failed only the guardian path on a missing `pdftotext` binary; it passed on rerun with the documented override and again in the final full run.
- Decisions and questions: D028 (band and marker semantics), D029 (freshness source, Today structure), OQ013 (cycle-phase hatching), OQ014 (reference hex mismatch, densities absent from code). No existing test assertion was changed.
- Cut: 200% text-scaling screenshots; Simple and Advanced densities (no mode exists); copy rewrites for middle-dot meta strings (visual-only pass). Screenshots are kept on disk under `design-audit/` and ignored by git because of their size; the manifest is tracked.

## Design system pass, founder round — 11 September 2026

- Built: Apple-like rounding and a floating tab bar (D030); three display densities stored per profile with onboarding choice, More → Display, Simple and Advanced Today and History, overlays, numeric baseline, formula disclosure and the raw daily table with source (D031, migrations 0035–0036); middle-dot meta strings rewritten; the duplicate live-status line removed; the alert-rules threshold editor collapsed behind the recommended defaults; DESIGN.md committed to the repository with the decisions folded in; cycle hatching approved (OQ013) and the urgent colour kept at the spec value because the reference's fails contrast (OQ014).
- Verified, in sequence on the final tree: `npm run check` (lint, strict typecheck, contrast check, 136 unit tests); `npm run build` (55 routes); `HMS_PDFTOTEXT=<Poppler pdftotext> FONTCONFIG_FILE=<Poppler fonts.conf> HMS_TEST_PORT=3102 npm run golden:verify` (10 passed, 4.2 min); `HMS_TEST_PORT=3101 npm run design:audit` (73 screenshots including the three densities and 200% text, no horizontal scroll, no page errors); `npm run db:verify` (92 passed after moving one expected rejection into its own case). Screenshots stay on disk under `design-audit/`; the manifest is tracked.
- Cut: none from the founder's list. Still open in DESIGN_AUDIT.md: Summary sparklines omit axis labels by design; the doctor's shared alert list uses plain cards.

## Timezone re-bucketing — 11 September 2026

- Built: post-import home timezone changes (OQ005, D032). Migrations 0037-0039 add `profiles.previous_timezone`/`timezone_changed_at`, `summary_jobs.rebucket` and `summary_snapshots.timezone`; `hms_private.change_timezone`/`enqueue_rebucket` queue every affected local day with a two-day margin; the `identity` and new `timezone` settings actions both route through it. The durable processor prunes only unacknowledged, non-escalated alerts older than 24 hours before re-evaluating under the new zone, then clears the flag. The patient view returns re-bucket progress and `stale_days`; History and Today show the progress notice and badge each day awaiting recalculation. `hms_private.clinical_summary` reports the snapshot's own timezone and flags a doctor's copy when the patient has since moved.
- Verified: `npm run lint`, `npm run typecheck`, `npm test` (136 unit tests); `npm run db:verify` (98 real Postgres tests, including an end-to-end re-bucket that moves five daily summaries onto the following local day while the raw readings stay put); `npm run db:migrate` applied 0037-0039; `npm run db:advisors` reported no issues; `npm run build`.
- Cut: the queue is drained by the owner-driven refresh batches, so a very long history needs several visits until the minute scheduler is deployed (OQ004). Acknowledged and escalated alerts are never pruned. Snapshots stay immutable.
- Remaining in this pass: the doctor's shared alert list still uses plain cards rather than the DESIGN.md §6 rule; consultation chat still uses explicit refresh.

## Doctor alert list and live consult chat — 11 September 2026

- Built: the doctor's shared alert list now uses the DESIGN.md §6 rule, icon and label, matching Today (D033), closing the last open DESIGN_AUDIT item other than the deliberate sparkline decision. Consultation chat gained a live channel (migration 0040): `hms_private.can_receive_consult_live` reuses the participant rule `consult_read` enforces, a `realtime.messages` policy scopes the `consult:<uuid>` topic, and triggers on `messages` and `consults` broadcast a ping carrying no clinical text, so the thread is always re-read through `hms_consult_read`.
- Verified: `npm run lint`, `npm run typecheck`, `npm test` (136 unit tests); `npm run db:verify` (98 real Postgres tests); `npm run db:migrate` applied 0040; `npm run db:advisors` reported no issues; `npm run build`; `npm run golden:verify -- doctor.spec.ts` (both doctor golden paths pass).
- Cut: live websocket delivery did not reach the browser in the golden run and was not isolated inside the time box, so the explicit Refresh messages button stays unconditional and the golden path still asserts the refresh-based flow. The same channel mechanism has been unverified on the patient view since migration 0024. Logged as OQ015.

## M0 maintenance — hosted CI database replay — 14 September 2026

- Built: the stock PostgreSQL CI bootstrap now provides the minimal provider-owned Realtime objects required to replay migrations 0024, 0026 and 0040. The fixture is inert and does not claim to verify websocket delivery.
- Verified: hosted GitHub Actions run `34883833639` passed both the `quality` and `database` jobs for commit `a0f1803`; the quality job ran lint, strict typecheck, 136 unit tests, build and Playwright E2E, and the database job replayed every migration and passed all 98 real PostgreSQL tests.
- Cut: no Realtime websocket claim is made. End-to-end live delivery remains OQ015.

## Realtime and production verification — 18 September 2026

- Built: History now subscribes to the same PHI-free private patient invalidation channel as Today. Today, History and consultation chat expose connection state through test-only data attributes. Added a two-user golden test that proves remote updates without refresh and keeps the manual chat refresh as a recovery control (D036, resolved OQ015).
- Verified: production Supabase contains all 41 migrations, both Realtime policies, five helper functions, five triggers and RLS on `realtime.messages`. `npm run check` (136 unit tests), `npm run build` (55 routes), `npm run db:verify` (98), `npm run db:advisors` (no issues), `npm run storage:setup`, `npm run e2e` (9), `npm run auth:verify` (2), `npm run ingestion:verify` (2, including 620,285-record import and zero-insert re-import), `npm run alerts:verify` (1), `npm run golden:verify` (11, including no-refresh Realtime), `npm run pdf:verify` (six one-page fixtures), and `npm run design:audit` (73 screenshots, no page errors or horizontal overflow) all pass.
- Cut: production cron responses, real inbox delivery and physical-device push remain launch configuration/acceptance work under OQ003–OQ004. Preview still lacks deliberate public Supabase configuration under OQ011.

## M8 follow-up — Realtime history and outage recovery — 18 September 2026

- Built: retained the concurrent `5029e8d` Realtime work and independently confirmed its no-refresh test. History now retains its selected range and pagination cursor during invalidations, updates selected-day details and uses fresh baseline values. Patient and consultation subscriptions re-read after joining/rejoining and browser network recovery, display offline state promptly, abort superseded reads and ignore disposed effects. Manual chat refresh remains available. Reconciled README deployment/chat claims, FINAL_REPORT provider status and the resolved OQ002/OQ015 references; preserved historical verification entries.
- Verified: `npm run check` (lint, strict typecheck, contrast checks and 136 unit tests), `npm run build` (55 routes), `HMS_TEST_PORT=3112 npm run golden:verify -- realtime.spec.ts patient.spec.ts doctor.spec.ts` (all five pass), and `git diff --check`. The expanded Realtime regression covers all three surfaces, a selected reading on the older page of 401 days, two-way messages and a message sent while the recipient browser is offline. Synthetic test accounts are cleaned up by the fixture. Initial follow-up attempts exposed an exact-label test selector mismatch and missing browser-offline handling; those failed attempts are not counted as passes.
- Cut: none from requested items 1–2. No schema migration or deployment was performed by this follow-up. Production notification, scheduler, Preview and external launch gates remain separately tracked.

## M7/M4 operations — Production, isolated Preview and minute dispatch — 18 September 2026

- Built: deployed runtime `6fbe1e6` to Production and added `playwright.deployed.config.ts` for explicit HTTPS testing without a local server. Created the founder-approved $0/month `hms-preview` project in the existing garilohia organisation, Mumbai; replayed all 41 migrations with Drizzle ledger and copied only 25 public catalogue rows. Preview has independent public Supabase settings and consent salt, with no production server/database credentials. Activated the Supabase minute dispatcher with matching restricted Vault/server secret and no duplicate Vercel schedule.
- Verified: `npm run check` (lint, strict typecheck, contrast and 136 unit tests), `npm run build` (55 routes), `HMS_DEPLOYMENT_URL=https://hms-indol-psi.vercel.app npx playwright test --config playwright.deployed.config.ts` (nine pass, including a repeat against the final alias in 28.2 seconds), `npm run cron:setup`, direct scheduler POST unauthorised 401/authenticated 200, and actual scheduled HTTP 200 responses at 20:11, 20:12 and 20:13 UTC on 17 September. Current alias `dpl_GLEjk1tBo3qpimszeZGuhFUJpqLz` was confirmed Ready at runtime revision `6fbe1e64a3c8686f5b9da72adb0d5cc46a328110`. Staging checks show 41 migrations, no profiles/metrics and RLS on every public table; advisor has only six informational deny-by-default no-policy notices, no warning/error findings.
- Failure/incident: `vercel env rm KEY preview --yes` deleted whole shared Production/Preview records. This was my operator error. Restored Production database/server credentials and Development direct URL, replaced cron secret and consent salt, and redeployed. Five Production Resend/VAPID settings remain unrecoverable through Vercel and require founder restoration; email/push are disabled/stubbed. The unused legacy Fitbit client ID was also deleted. One scheduled HTTP 401 during a concurrent deployment was followed by consecutive 200s. Full recovery and real notification delivery are not claimed.
- Cut/blocked: Preview server/database credentials, Storage, synthetic seeding, redirects/origin and deployed acceptance await Supabase dashboard access. The full deployed golden command (`HMS_DEPLOYMENT_URL=https://hms-indol-psi.vercel.app HMS_DEPLOYED_ENV_FILE=.env.local HMS_DEPLOYED_SUITE=golden npx playwright test --config playwright.deployed.config.ts`) is not green: sample summary deadline, snapshot confirmation, reconnect/status assertion and sign-in navigation failures are tracked in OQ016. Another task concurrently deployed and ran production tests; its files/processes were preserved. No assertions were relaxed. See `docs/verification/deployment-2026-09-18.md`, OQ004 and OQ011 before continuing.
- Final deployed golden result: six passed, five failed in 14.9 minutes. Realtime on Today/History/chat, attention acknowledgement, export/deletion, deletion recovery, catalogue and integration boundaries passed. Caregiver/guardian summary deadlines, doctor snapshot/status checks and persona-b sign-in timed out/failed. Cleanup blocks completed without reported cleanup errors. This is not a full production acceptance pass.

## M4 operations — Notification configuration recovery — 18 September 2026

- Built: founder restored Resend settings and authorised a replacement VAPID pair. Generated/validated one pair using installed `web-push`; saved the private key as sensitive, Production-only, without printing or storing it locally. Set the public key and production HTTPS origin as subject. Rebuilt the existing deployed source using `vercel redeploy dpl_GLEjk1tBo3qpimszeZGuhFUJpqLz --target production --no-wait`, leaving concurrent test edits untouched.
- Verified: `npm run build` (55 routes), `npm run check` (136 unit tests), Ready deployment `dpl_Cac6aMw5jAnoAhncfFxJHpd3dQX9` at the public production alias, and metadata confirming all five Resend/VAPID names in the deployment snapshot. `HMS_DEPLOYMENT_URL=https://hms-indol-psi.vercel.app npx playwright test --config playwright.deployed.config.ts` passed all nine tests in 19.5 seconds.
- Cut: real inbox and physical-device delivery remain unverified. No notification was sent by these checks. Existing browser subscriptions require Disable then Enable instant alerts. Earlier authenticated acceptance failures and Preview credential blockers remain open; notification restoration does not resolve them.

## M7 follow-up — Co-locate deployment with data — 18 September 2026

- Built: `vercel.json` selects Mumbai (`bom1`) instead of Washington for server functions, keeping the database and single-region plan unchanged (D037). Preserved another task's uncommitted doctor failure-injection test fix.
- Verified before deployment: `npm run check` (136 tests) and `npm run build` (55 routes). Deployed acceptance is pending below; no test timeouts were increased.
- Preview access: browser sign-in succeeded but the account has only a different organisation; Supabase explicitly denied access to `hms-preview`. Requested switching to the existing garilohia owner account. No new account or organisation was created.

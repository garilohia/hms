# Shared provider allowance — 19 September 2026

## Built

- Migration 0042 adds a private, forced-RLS token bucket with server-only INVOKER reservation/cooldown functions. Its fixed scopes contain no patient/account identities or health values. Reservation uses database time after locking and commits before HTTP; uncertain requests are never refunded.
- D043 records conservative WHOOP client and Google project limits, configuration requirements and cross-deployment boundaries. Missing budget configuration/database blocks outbound requests. This does not establish real-provider acceptance or a minute-level capacity claim.
- D044 adds first-failure stopping to deployed acceptance and bounded, redacted caregiver request diagnostics. No test deadline or substantive assertion changed. Hosted failure evidence for runtime `0d9bcf6` is in [the recovery report](recovery-2026-09-19.md).

## Verification

- `npm run db:verify -- tests/integration/provider-budgets.test.ts`: all eleven pass in 6.37 seconds. Covers concurrent last-permit consumption, server-only permissions, fixed burst/refill bounds, backwards-clock safety, long-idle cap, concurrent cooldown persistence and invalid inputs. The suite creates only an isolated, random quota-test schema and removes it after verification; it creates no Auth or patient records.
- `npm run db:verify`: all 115 tests pass in 181.59 seconds, including full migration replay and the existing access/consent/clinical-data boundaries.
- The first focused run had ten passes and one fixture timestamp mismatch: the database driver's timestamptz parameter conversion truncated microseconds. Passing the synthetic deadline as text before the SQL cast preserves the exact value. The SQL algorithm did not change to accommodate that assertion.
- Read-only peer review found no SQL concurrency, permissions or test-isolation blocker.
- Final `npm run check`: lint, strict TypeScript, contrast and all 366 unit tests pass. Final `npm run build`: all 55 routes build. Before the final URL-mutation regression, the focused provider suite passed all 141 tests across ten files. An interim unit run overlapped wiring before its mocks were updated and is not counted as a passing quality gate.
- Applied migration 0042 through `npm run db:migrate` to verified Production and through the connected migration API plus matching Drizzle checksum/ledger to Preview. Both now have 43 migrations and an empty budget table with forced RLS and no `anon`, `authenticated` or `service_role` read privilege. Preview still contains zero profiles/metrics. No data was copied.
- Security advisors after migration: Preview has seven informational deny-by-default/no-policy notices, no warnings/errors. Production has those informational notices and the two previously recorded OQ017 warnings. Do not relocate the managed extension blindly: [extension guidance](https://supabase.com/docs/guides/database/database-linter?lint=0014_extension_in_public), [password protection](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).
- TypeScript wiring review found the documented WHOOP token-exchange/identity failure boundary: permission can exist at the provider before HMS has a completed connection. The failure notice now explains manual provider-side removal; the regression rejects a locally denied second request without returning a successful grant. D043/OQ012 retain authorised acceptance for this path. No automatic cleanup of identity-less grants is claimed.
- `HMS_TEST_PORT=3112 HMS_PDFTOTEXT=/Users/gari/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/poppler/poppler/bin/pdftotext FONTCONFIG_FILE=/Users/gari/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/poppler/poppler/etc/fonts/fonts.conf caffeinate -i npm run golden:verify -- --max-failures=1 --output test-results/quota-golden-local`: all eleven pass together in 5.7 minutes. `HMS_TEST_PORT=3113 npm run e2e`: all nine public/boundary tests pass in 18.5 seconds. Notifications remain explicitly stubbed. No fixture cleanup error was reported, and a subsequent read-only check finds zero `hms-family-…@example.com` accounts created since the earlier interrupted run.
- The caregiver timing attachment reports its `/more/alerts` document returned 200, with headers after 413 milliseconds and completion after 453 milliseconds. Its 390-pixel alert screenshot was inspected. This is local-server evidence, not a fix or latency guarantee for the previous hosted failure.
- Final review then hardened the wrapper against caller mutation of a validated URL object during awaited admission. Its regression passes in the final 366-test gate and the build was repeated. This change does not enable real OAuth in the browser fixtures; authorised provider acceptance remains outstanding.
- Deployment dry-run inspection selected 421 files/6,240,865 bytes at that point, with zero private credential, test-result, design-PNG or TypeScript-cache matches. Later edits were source/documentation only. No source was uploaded by the dry run.
- Release evidence follows after commit and CI; hosted signed-in acceptance remains OQ016 and is not being repeatedly rerun until green.

## Still required

Actual provider approval, configured client/project identity, approved quota and authorised account/device acceptance; measured scheduling fairness and source-to-recipient delay; verified sending-domain/inbox and physical-device notifications; correct-account isolated Preview setup; supported Supabase security hardening; clinical/legal review. Native binaries, signing and physical-device acceptance stay outside this web implementation's scope. No real notification, provider account, paid service or terms acceptance was performed.
